# Variables de entorno de marcelo-store.
# Copiá este archivo a `.env.local` y completá los valores reales.
# NUNCA commitees `.env` ni `.env.local` (están en .gitignore).
#
# Todavía no hay variables requeridas: el scaffold inicial no consume ningún
# servicio externo. A medida que se integren Google Sheets y Mercado Pago
# (tickets siguientes) se van a agregar acá con una descripción de cada una.
